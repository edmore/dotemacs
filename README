To setup your configuration do the following on Mac OS / Linux :

1. Navigate to the home directory (~/) on your machine
2. git clone git@github.com:edmore/dotfiles.git
3. cd dotfiles/
4. Run ./setup.rb

